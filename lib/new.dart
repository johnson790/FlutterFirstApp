import 'package:flutter/material.dart';

class New extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Your widget's UI goes here
    return Container(
      //Define your widget's appearance
      child: Text('Hello,Custom Widget!'),
    );
  }
}
